# Pose_Detection
This project uses real-time pose detection with MediaPipe and OpenCV to track arm movement and calculate angles using Python. The calculated angle is sent to Arduino, which controls a servo motor to move according to the user’s arm position. It demonstrates the integration of computer vision and hardware for real-time gesture-based control systems.
